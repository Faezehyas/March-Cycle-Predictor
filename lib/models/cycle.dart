class Cycle {
  DateTime startDate;
  int cycleLength;

  Cycle({required this.startDate, required this.cycleLength});

  // You can add methods here to calculate the next cycle dates based on this data.
}
